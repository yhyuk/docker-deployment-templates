# Server Configuration
SERVER_PORT=8080

# Spring Profile
SPRING_PROFILE=prod

# Database Configuration
DB_NAME=app_db
DB_USER=root
DB_PASSWORD=password
DB_PORT=3306

# JPA Configuration
JPA_DDL_AUTO=validate
JPA_SHOW_SQL=false
